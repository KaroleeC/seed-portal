/**
 * @deprecated Use `import { brand } from '@/assets'` instead
 * This file is kept for backwards compatibility only
 */

// Re-export from centralized asset registry
export { 
  brand, 
  seedos, 
  apps, 
  misc,
  logoLight,
  logoDark,
  logoLightFallback,
  logoDarkFallback,
} from './index';
