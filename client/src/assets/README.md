This folder stores static assets used by the client app.

- Place the assistant avatar as `assistant-bot.png` here.
- Example path (Vite alias): `@assets/assistant-bot.png`
- Recommended size: 128x128 to 256x256 PNG, with circular safe area.
